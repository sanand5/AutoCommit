
Esta es la salida de mi `git diff HEAD`. Quiero que generes un único commit que englobe todos los cambios en castellano, utilizando títulos y mensajes apropiados para cada diff. 
- La salida se debe de poder ejectar en la consola
- No debes incluir ningun texto adicional
- La salida debe seguir estrictamente este formato: git commit -m "título descriptivo del commit" -m "mensaje del commit"
- El título puede ser en inglés pero la descripción debe ser en castellano.
Solo debes generar una unica salida

Ruta: a/SBD/Sessio_05/MongoDB_consultas.md
Cambios:
index 4a3ec816..df8c772b 100644
--- a/SBD/Sessio_05/MongoDB_consultas.md
+++ b/SBD/Sessio_05/MongoDB_consultas.md
@@ -1,23 +1,19 @@
 ## Consultas / Buscar documentos
 1. **Obtener todos los documentos**
-`db["movies"].find()`
+```JavaScript
+db.movies.find()
+```
 2. **Obtener documentos con `writer` igual a `"Quentin Tarantino"`**
 ```JavaScript
-db.movies.aggregate([
-  { $match: { "writer": "Quentin Tarantino" } }
-]);
+db.movies.find({writer : "Quentin Tarantino"})
 ```
 3. **Obtener documentos con `actors` que incluyan a `"Brad Pitt"`**
 ```JavaScript
-db.movies.aggregate([
-  { $match: { "actors": "Brad Pitt" } }
-]);
+db.movies.find({actors : "Brad Pitt"})
 ```
 4. **Obtener documentos con `franchise` igual a `"The Hobbit"`**
 ```JavaScript
-db.movies.aggregate([
-  { $match: { "franchise": "The Hobbit" } }
-]);
+db.movies.find({franchise : "The Hobbit"})
 ```
 5. **Obtener todas las películas de los 90s ordenadas por `year`**
 ```JavaScript
